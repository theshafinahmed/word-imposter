<!--
TEMPLATE — this is the project's own memory index (see
.claude/rules/project-memory.md). It mirrors Claude Code's built-in auto
memory format (MEMORY.md index + one topic file per memory) but lives here,
inside the project, so it's committed to git and travels with the project
instead of staying on one machine.

Clear the example line below when a real project starts from this template.
Keep one line per entry, newest-relevant on top, pointing to a topic file
for the detail.
-->

# Project memory index

- [Word Imposter v1 product-rule decisions](word_imposter_v1_decisions.md) — imposter reveal, validation, mascot pick, etc. — settled 2026-09-18
